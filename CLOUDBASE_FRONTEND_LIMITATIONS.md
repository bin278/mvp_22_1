# CloudBase前端SDK功能限制说明

## 🎯 前端SDK vs 服务端SDK

### 前端SDK (Web SDK)
**用途**: 浏览器中使用，提供用户认证、数据库查询等功能
**限制**: 出于安全考虑，不提供敏感操作如发送邮件、短信等

### 服务端SDK (Node.js SDK)
**用途**: 云函数中使用，提供完整的管理功能
**权限**: 可以发送邮件、短信、管理用户等

## 🚫 为什么前端不支持发送验证码

### 安全考虑
- **防止滥用**: 前端代码可见，密钥可能泄露
- **频率限制**: 难以控制发送频率和防止刷短信
- **成本控制**: 服务商需要控制发送成本

### 架构设计
- **服务端处理**: 敏感操作在服务端完成
- **权限隔离**: 前端只负责UI和服务调用
- **审计追踪**: 服务端可以记录所有操作

## ✅ 支持的前端功能

### 用户认证
- ✅ 邮箱密码注册
- ✅ 邮箱密码登录
- ✅ 用户信息查询
- ✅ 令牌管理

### 数据库操作
- ✅ 数据查询 (select)
- ✅ 数据插入 (add)
- ✅ 数据更新 (update)
- ✅ 数据删除 (remove)

### 文件存储
- ✅ 文件上传
- ✅ 文件下载
- ✅ 文件删除

## ❌ 不支持的前端功能

### 邮件发送
- ❌ 发送验证码邮件
- ❌ 发送通知邮件
- ❌ 发送营销邮件

### 短信发送
- ❌ 发送验证码短信
- ❌ 发送通知短信

### 用户管理
- ❌ 批量创建用户
- ❌ 删除用户账户
- ❌ 修改用户权限

### 敏感配置
- ❌ 修改环境配置
- ❌ 访问系统日志

## 🛠️ 解决方案：使用云函数

### 为什么需要云函数
```javascript
// ❌ 前端直接发送（不支持）
const auth = app.auth();
await auth.sendEmailCode(email); // 错误：方法不存在

// ✅ 通过云函数发送（推荐）
const result = await app.callFunction({
  name: 'sendEmailVerification',
  data: { email: email }
});
```

### 云函数优势
- **安全性**: 密钥存储在服务端
- **可控性**: 可以添加频率限制、验证等
- **可扩展**: 可以集成第三方邮件服务
- **可监控**: 完整的日志和错误追踪

## 📋 实现步骤

### 1. 创建云函数
```javascript
// cloud-functions/sendEmailVerification/index.js
const cloudbase = require('@cloudbase/node-sdk');

exports.main = async (event, context) => {
  const { email } = event;

  // 初始化（使用环境变量）
  const app = cloudbase.init({
    env: process.env.TENCENT_CLOUD_ENV_ID,
    secretId: process.env.TENCENT_CLOUD_SECRET_ID,
    secretKey: process.env.TENCENT_CLOUD_SECRET_KEY,
  });

  // 发送验证码
  const auth = app.auth();
  await auth.sendEmailCode(email);

  return { success: true };
};
```

### 2. 前端调用
```javascript
// 前端调用云函数
const result = await app.callFunction({
  name: 'sendEmailVerification',
  data: { email: userEmail }
});
```

### 3. 部署云函数
```bash
# 使用CloudBase CLI
cloudbase functions:deploy sendEmailVerification

# 或在控制台手动部署
```

## 🎯 最佳实践

### 前端负责
- 用户界面
- 表单验证
- 用户体验
- 调用云函数

### 云函数负责
- 发送邮件/短信
- 数据验证
- 业务逻辑
- 权限检查

### 安全建议
- 云函数中验证用户身份
- 添加频率限制
- 记录操作日志
- 使用HTTPS调用

## 📚 相关资源

- [CloudBase Web SDK文档](https://docs.cloudbase.net/api-reference/webv2/)
- [CloudBase Node.js SDK文档](https://docs.cloudbase.net/api-reference/nodejs/)
- [云函数开发指南](https://docs.cloudbase.net/cloud-function/)

## 💡 总结

**前端SDK不支持发送验证码是设计使然**，这是为了保证安全性和可控性。我们应该使用云函数来处理这类敏感操作，这样既安全又灵活。




